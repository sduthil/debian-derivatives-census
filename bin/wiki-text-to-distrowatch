#!/bin/sh

# Copyright 2011 Paul Wise
# Released under the MIT/Expat license, see doc/COPYING

set -e
sed -r -n -e 's_^ *\* *Distrowatch *[^:]*: http://distrowatch\.com/(table\.php\?distribution=)?(.*)_\2_p' "$1" | tr ' ' '\n' > "$2"
find "$2" -empty -delete
