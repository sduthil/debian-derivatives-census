#!/bin/sh
set -e
. etc/census.conf

wget -O $wiki_dir/CensusTemplate.txt http://wiki.debian.org/Derivatives/CensusTemplate?action=raw
for u in $(cat $wiki_urls) ; do
	wget -O - "$u?action=raw" | tr -d '\r' > $wiki_dir/$(basename $u).txt
done
